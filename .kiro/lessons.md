# Lessons

Raw, timestamped corrections — one entry per actual mistake, written the moment it's corrected. This is a staging log, not steering: it is not loaded automatically. The `patterns-consolidation` skill reviews it periodically and promotes anything reinforced into `user-patterns.md` or `working-style.md`; promoted entries are removed from here rather than left to accumulate.

Format: `- [date] <what was wrong> → <the rule that prevents it> (context: <why>)`

(No entries yet.)
